/**
 * version.js – Central application version constant.
 *
 * Bump APP_VERSION with every release. index.html automatically
 * appends it as a query string to all static assets (style.css,
 * data.js, app.js), busting the browser cache for every new version.
 */

const APP_VERSION = '1.0.0';
